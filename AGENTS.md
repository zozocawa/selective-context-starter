# [Project Name]

[One-line description of the project]

## On-demand context (load via `read` when task matches domain)

| File | When |
|------|------|
| `path/to/domain-a.md` | Working on [Domain A] |
| `path/to/domain-b.md` | Debugging [Component B] |
| `path/to/domain-c.md` | Deploying to [Environment C] |

## Root commands

| Command | What it does |
|---------|-------------|
| `npm run dev` | Start dev server |
| `npm run build` | Build for production |
| `npm run test` | Run tests |

## State tracking (mandatory)

- `docs/spec/PROJECT_SPEC.md` is the authoritative spec — all work must conform.
- `docs/spec/PROJECT_STATE.md` must be updated after every code change to reflect current reality.
- `docs/spec/PROTOCOL.md` — operating protocol, follow always.
